[pytest]
asyncio_mode = auto
markers = 
    only: only this test shall run
filterwarnings =
    ignore::DeprecationWarning:nptyping.*
# log_cli = False
# log_cli_level = INFO
# log_file = test_info.log.jsonl
# log_file_level = INFO
